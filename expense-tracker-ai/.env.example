# Copy this file to .env and fill in your key (or set as a Hugging Face Space "Secret")
# Get a free Groq API key at https://console.groq.com
# If left unset, the app falls back to a regex-based parser automatically —
# the app still works end to end, just with slightly dumber text parsing.
GROQ_API_KEY=
