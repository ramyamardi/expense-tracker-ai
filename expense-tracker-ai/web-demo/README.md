# Web demo (no deployment needed)

`Hearth.jsx` is a self-contained React version of this project. It runs
entirely client-side with persistent browser storage and real AI parsing —
useful as a zero-setup demo link, separate from the full Gradio + SQLite app
in the parent folder (which is the version with the real ETL pipeline,
database, and is meant for deployment to Hugging Face Spaces).
